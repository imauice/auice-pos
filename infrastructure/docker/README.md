# Development services

From the repository root, start MongoDB and Valkey with `docker compose up -d`. Inspect state with `docker compose ps`, follow output with `docker compose logs`, and stop containers with `docker compose down`. Named volumes retain development data; no production credentials are configured.

